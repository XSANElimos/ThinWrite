import 'package:flutter/material.dart';

class WriterPage extends StatelessWidget {
  const WriterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Writer'),
    );
  }
}
